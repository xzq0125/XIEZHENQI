<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:orientation="vertical">

    <am.widget.stateframelayout.StateFrameLayout
        android:id="@+id/common_pull_down_refresh_loading"
        android:layout_width="match_parent"
        android:layout_height="50dp">

    </am.widget.stateframelayout.StateFrameLayout>

    <TextView
        android:id="@+id/common_pull_down_refresh_ok"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:alpha="0"
        android:drawableLeft="@drawable/ic_ok"
        android:drawablePadding="5dp"
        android:drawableStart="@drawable/ic_ok"
        android:text="刷新成功"
        android:textColor="@color/themeColor"
        android:textSize="11sp"
        android:textStyle="bold" />

    <ImageView
        android:id="@+id/common_pull_down_refresh_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:src="@drawable/ic_arrow" />

</RelativeLayout>